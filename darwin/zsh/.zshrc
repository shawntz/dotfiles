# ── dotfiles (macOS / darwin) zshrc ──────────────────────────────────────────
source ~/.zsh/rc

. "$HOME/.cargo/env"# >>> dotfiles bootstrap (env) >>>
# Exported by dotfiles installer
export USER_NAME="Shawn Schwartz"
export USER_EMAIL="shawn@shawnschwartz.com"
export USER_SITE="https://shawnschwartz.com"
# <<< dotfiles bootstrap (env) <<<
